const Title = ({ children }) => (
  <h1 style={{
    textAlign: "center",
    fontSize: "2.2rem",
    textTransform: "uppercase",
    letterSpacing: "2px",
    color: "rgb(19, 228, 141)",
    marginBottom: "1rem",
    marginTop: "1rem",
  }}>{children}</h1>
)

export default Title